### Update dependencies

Automatically created PR from [`ci/dependabot-updates`]({{ cookiecutter.scm_url }}/tree/ci/dependabot-updates).

For more information see the ["Dependabot updates" workflow]({{ cookiecutter.scm_url }}/blob/master/.github/workflows/ci_dependabot.yml).

#### To-do

- [ ] Check that the diff is sensible, and that tests and builds pass with the new dependency versions.
- [ ] Make sure that the PR is **squash** merged, with a sensible commit message.
